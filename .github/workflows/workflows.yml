name: Node CI

on:
  - push
  - pull_request

jobs:
  build:
    # Set the type of machine to run on
    runs-on: ubuntu-latest
    steps:
      - name: run linter
        # Run Linter
        run: |
          make lint
